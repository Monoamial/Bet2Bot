{
  "name": "bet2bot-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "bundle-engine": "node scripts/bundle_engine.mjs",
    "bundle-runtime": "node scripts/bundle_runtime.mjs",
    "bundle-all": "npm run bundle-engine && npm run bundle-runtime",
    "predev": "npm run bundle-all",
    "dev": "vite",
    "prebuild": "npm run bundle-all",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "pyodide": "^0.26.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
